version: '3.8'

services:
  api:
    build: .
    container_name: chrono26_backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - ENV=development
    restart: always

# Usage:
# Run 'docker-compose up --build' to start the engine
